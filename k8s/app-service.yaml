apiVersion: v1
kind: Service
metadata:
  name: bank-app-service
  namespace: default
spec:
  selector:
    app: bank-app
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
  type: NodePort
